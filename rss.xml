---
layout: null
title : RSS Feed
---

<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
<channel>
        <title>{{ site.title }}</title>
        <description>{{ site.title }} - {{ site.author.name }}</description>
        <link>{{ site.production_url }}{{ site.rss_path }}</link>
        <link>{{ site.production_url }}</link>
        <lastBuildDate>{{ site.time | date_to_xmlschema }}</lastBuildDate>
        <pubDate>{{ site.time | date_to_xmlschema }}</pubDate>
        <ttl>1800</ttl>

 {% comment %}
   Papers only. These two feeds predate the blog and are what people have
   subscribed to for years; a bare site.posts loop pushed blog posts into
   them. Blog posts have their own feed at /blog/feed.xml.
 {% endcomment %}
{% assign feed_posts = site.posts | where: 'category', 'paper' %}
{% for post in feed_posts %}
        <item>
                <title>{{ post.title }}</title>
                <description>{{ post.content | xml_escape }}</description>
                <link>{{ site.production_url }}{{ post.url }}</link>
                <guid>{{ site.production_url }}{{ post.id }}</guid>
                <pubDate>{{ post.date | date_to_xmlschema }}</pubDate>
        </item>
{% endfor %}

</channel>
</rss>
